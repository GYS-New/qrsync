'use client'

import GorevOlusturClient from '@/components/oto-yikama/GorevOlusturClient'

export default function GorevOlusturPageClient({ firmaId }: { firmaId: string }) {
  return <GorevOlusturClient firmaId={firmaId} />
}
